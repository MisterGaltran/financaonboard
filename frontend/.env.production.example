# Variáveis de ambiente do frontend em PRODUÇÃO (Cloudflare Pages)
# Copiar estes valores para o painel do Cloudflare Pages em:
#   Settings → Variables and Secrets → Production
#
# Vite injeta no bundle em build-time. Mudou aqui? Precisa rebuild.
#
# A URL do backend mudou conforme a estratégia evoluiu:
#   v1: https://davi-mendes.dedyn.io          (Caddy + dedyn.io — bloqueado pelo ISP residencial)
#   v2: https://yellow-vpn.<tailnet>.ts.net   (Tailscale Funnel — em uso atualmente)
#
# Substitua <tailnet> pelo identificador do seu tailnet (descubra com: tailscale funnel status)

VITE_BACKEND_URL=https://yellow-vpn.tailf47dec.ts.net

VITE_WS_RECONNECT_DELAY=1000
VITE_WS_RECONNECT_DELAY_MAX=15000
